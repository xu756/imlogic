// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"imlogic/ent/groupmessage"
	"imlogic/kitex_gen/im"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// GroupMessage is the model entity for the GroupMessage schema.
type GroupMessage struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 消息类型
	MsgType int32 `json:"msg_type,omitempty"`
	// 消息id
	MsgID string `json:"msg_id,omitempty"`
	// 群id
	GroupID int64 `json:"group_id,omitempty"`
	// 消息时间戳
	Timestamp int64 `json:"timestamp,omitempty"`
	// 发送者id
	SenderID int64 `json:"sender_id,omitempty"`
	// 消息内容
	Content      *im.Message `json:"content,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*GroupMessage) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case groupmessage.FieldContent:
			values[i] = new([]byte)
		case groupmessage.FieldID, groupmessage.FieldMsgType, groupmessage.FieldGroupID, groupmessage.FieldTimestamp, groupmessage.FieldSenderID:
			values[i] = new(sql.NullInt64)
		case groupmessage.FieldMsgID:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the GroupMessage fields.
func (gm *GroupMessage) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case groupmessage.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			gm.ID = int(value.Int64)
		case groupmessage.FieldMsgType:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field msg_type", values[i])
			} else if value.Valid {
				gm.MsgType = int32(value.Int64)
			}
		case groupmessage.FieldMsgID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field msg_id", values[i])
			} else if value.Valid {
				gm.MsgID = value.String
			}
		case groupmessage.FieldGroupID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field group_id", values[i])
			} else if value.Valid {
				gm.GroupID = value.Int64
			}
		case groupmessage.FieldTimestamp:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field timestamp", values[i])
			} else if value.Valid {
				gm.Timestamp = value.Int64
			}
		case groupmessage.FieldSenderID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field sender_id", values[i])
			} else if value.Valid {
				gm.SenderID = value.Int64
			}
		case groupmessage.FieldContent:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field content", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &gm.Content); err != nil {
					return fmt.Errorf("unmarshal field content: %w", err)
				}
			}
		default:
			gm.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the GroupMessage.
// This includes values selected through modifiers, order, etc.
func (gm *GroupMessage) Value(name string) (ent.Value, error) {
	return gm.selectValues.Get(name)
}

// Update returns a builder for updating this GroupMessage.
// Note that you need to call GroupMessage.Unwrap() before calling this method if this GroupMessage
// was returned from a transaction, and the transaction was committed or rolled back.
func (gm *GroupMessage) Update() *GroupMessageUpdateOne {
	return NewGroupMessageClient(gm.config).UpdateOne(gm)
}

// Unwrap unwraps the GroupMessage entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (gm *GroupMessage) Unwrap() *GroupMessage {
	_tx, ok := gm.config.driver.(*txDriver)
	if !ok {
		panic("ent: GroupMessage is not a transactional entity")
	}
	gm.config.driver = _tx.drv
	return gm
}

// String implements the fmt.Stringer.
func (gm *GroupMessage) String() string {
	var builder strings.Builder
	builder.WriteString("GroupMessage(")
	builder.WriteString(fmt.Sprintf("id=%v, ", gm.ID))
	builder.WriteString("msg_type=")
	builder.WriteString(fmt.Sprintf("%v", gm.MsgType))
	builder.WriteString(", ")
	builder.WriteString("msg_id=")
	builder.WriteString(gm.MsgID)
	builder.WriteString(", ")
	builder.WriteString("group_id=")
	builder.WriteString(fmt.Sprintf("%v", gm.GroupID))
	builder.WriteString(", ")
	builder.WriteString("timestamp=")
	builder.WriteString(fmt.Sprintf("%v", gm.Timestamp))
	builder.WriteString(", ")
	builder.WriteString("sender_id=")
	builder.WriteString(fmt.Sprintf("%v", gm.SenderID))
	builder.WriteString(", ")
	builder.WriteString("content=")
	builder.WriteString(fmt.Sprintf("%v", gm.Content))
	builder.WriteByte(')')
	return builder.String()
}

// GroupMessages is a parsable slice of GroupMessage.
type GroupMessages []*GroupMessage
